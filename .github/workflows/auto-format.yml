name: Автоматическое форматирование кода

on:
  pull_request:
    branches: [ main, develop ]
    types: [opened, synchronize]

jobs:
  auto-format:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    
    steps:
    - name: Получение кода
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        ref: ${{ github.event.pull_request.head.ref }}
    
    - name: Настройка Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Установка инструментов форматирования
      run: |
        python -m pip install --upgrade pip
        pip install black isort
    
    - name: Проверка необходимости форматирования
      id: check-format
      run: |
        # Проверяем, нужно ли форматирование
        if ! black --check --diff . > /dev/null 2>&1; then
          echo "needs_black=true" >> $GITHUB_OUTPUT
        else
          echo "needs_black=false" >> $GITHUB_OUTPUT
        fi
        
        if ! isort --check-only --diff . > /dev/null 2>&1; then
          echo "needs_isort=true" >> $GITHUB_OUTPUT
        else
          echo "needs_isort=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Форматирование кода с Black
      if: steps.check-format.outputs.needs_black == 'true'
      run: |
        echo "🎨 Форматирование кода с Black..."
        black .
    
    - name: Сортировка импортов с isort
      if: steps.check-format.outputs.needs_isort == 'true'
      run: |
        echo "📦 Сортировка импортов с isort..."
        isort .
    
    - name: Проверка изменений
      id: changes
      run: |
        if git diff --quiet; then
          echo "has_changes=false" >> $GITHUB_OUTPUT
        else
          echo "has_changes=true" >> $GITHUB_OUTPUT
        fi
    
    - name: Коммит изменений
      if: steps.changes.outputs.has_changes == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "🎨 Автоматическое форматирование кода
        
        - Применено форматирование Black
        - Отсортированы импорты с isort
        
        Co-authored-by: github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
    
    - name: Отправка изменений
      if: steps.changes.outputs.has_changes == 'true'
      run: |
        git push
    
    - name: Комментарий в PR об автоматическом форматировании
      if: steps.changes.outputs.has_changes == 'true'
      uses: actions/github-script@v6
      with:
        script: |
          const comment = `## 🎨 Автоматическое форматирование кода
          
          Код был автоматически отформатирован для соответствия стандартам проекта:
          
          ### ✅ Применены изменения:
          ${steps['check-format'].outputs.needs_black === 'true' ? '- 🎨 Форматирование с Black\n' : ''}
          ${steps['check-format'].outputs.needs_isort === 'true' ? '- 📦 Сортировка импортов с isort\n' : ''}
          
          ### 📋 Что это означает:
          - Код теперь соответствует стилю проекта
          - Все проверки форматирования должны пройти успешно
          - Изменения были автоматически добавлены в ваш PR
          
          ### 🔄 Следующие шаги:
          1. Обновите вашу локальную копию: \`git pull\`
          2. Проверьте, что все изменения корректны
          3. При необходимости внесите дополнительные правки
          
          ---
          *Автоматически создано GitHub Actions*
          `;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });
    
    - name: Уведомление об отсутствии изменений
      if: steps.changes.outputs.has_changes == 'false'
      run: |
        echo "✅ Код уже соответствует стандартам форматирования"